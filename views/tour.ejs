<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= tour.name %> - SinkTravel</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <nav>
    <p>
      <img id="icon" src="/images/icon.svg">
      <button id="takeTour">Подобрать тур</button>
    </p>
    <% if (user) { %>
    <p>
      <img id="active" src="/images/search.svg">
      <img id="active" src="/images/cart.svg">
      <img id="active" src="/images/profile.svg" onclick="window.location.href='/logout'">
    </p>
    <% } else { %>
    <p>
      <img id="active" src="/images/search.svg">
      <button onclick="window.location.href='/login'">Войти</button>
    </p>
    <% } %>
  </nav>

  <div class="tour-detail-container">
    <section>
      <img src="/images/abkhazia.png" class="tour-image-large">
    </section>

    <section>
      <h1><%= tour.name %></h1>
      <p><strong>Описание:</strong> <%= tour.description %></p>

      <div id="cardTable">
        <section>
          <h3>Тип</h3>
          <p><%= tour.type || 'не указано' %></p>
        </section>
        <section>
          <h3>Виза</h3>
          <p><%= tour.visaRequired ? 'Нужна' : 'Не нужна' %></p>
        </section>
        <section>
          <h3>Перелёт</h3>
          <p><%= tour.flightType || 'не указан' %></p>
        </section>
      </div>

      <div id="cardTable">
        <section>
          <h3>Цены на месте</h3>
          <p><%= tour.onSpotPriceType || 'не указано' %></p>
        </section>
        <section>
          <h3>Время поездки</h3>
          <p><%= tour.tripTime || 'не указано' %></p>
        </section>
        <section>
          <h3>Особенность</h3>
          <p><%= tour.features || 'не указано' %></p>
        </section>
      </div>

      <div class="bottom-panel">
        <div class="tour-price"><%= tour.price.toFixed(2) %>₽</div>
        <div><button>Купить</button></div>
      </div>
    </section>
  </div>

  <footer>
    <p>
      ©SinkTravel
      <a href="/catalog">Каталог</a>
      <a href="/take-tour">Подобрать тур</a>
      <a href="/cart">Корзина</a>
      <a href="/search">Поиск</a>
      <a href="/profile">Профиль</a>
    </p>
  </footer>
</body>
</html>